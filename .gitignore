# Dependencies
node_modules/
node_modules
node_modules/*
.npm/

# Build and Output
dist/
dist-electron/
release/
output/
.remotion/
*.remotion
/workspace/
workspace/
workspace/staging/
workspace/tmp/
workspace/cache/
coverage/

# Environment Variables
.env
.env.local
.env.*.local

# Media and Logs
*.mp4
*.mp3
*.wav
*.webm
*.avi
*.mov
*.log
logs.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Application Input
input/visuals/
input/bgm/
input/voiceover/

# OS and IDE files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
**/desktop.ini

# Cache and Temp
.video-cache.json
.mcp-jobs.json
*.tmp
*.bak
tmp/
temp/
.cache/

# Portable Runtime
portable-python/

# Regenerable build artifacts (recreated by scripts/create-icons.cjs)
assets/icons/tray-icon.png
assets/icons/icon-256.png
assets/icons/icon.ico

# TypeScript
*.tsbuildinfo

# Remotion study-only clones (MIT reference repos, NOT project source — do not commit/push)
remotion/_study/

# Docker
.docker/

# local backup copies (not source)
input/scripts/input-scripts-backup.json
input/scripts/input-scripts.json.bak-*

# Agentic pipeline runtime job workspaces (generated at runtime, NOT source)
# Each agentic job downloads candidates + writes verification/manifests here.
# Planning docs in agentic-pipeline/ (README.md, VERIFICATION_MATRIX.md,
# AGENT_TOOLS_AND_PHASES.md) and the source under src/agentic/ stay tracked.
agentic-pipeline/workspaces/
agentic-pipeline/.asset-cache/
agentic-pipeline/**/*.candidate.*
*.candidate.jpg
*.candidate.mp4
*.candidate.mp3

# Voicebox generated test clips (artifacts, not source)
voicebox-test-output/

# Temporary downloaded assets and runtime generator directories
images/
videos/
music/
image_*
video_*
audio_*
music_*
.hermes-*
.hermes-tmp*

# Sub-modules
sub-modules/*/node_modules/
sub-modules/*/dist/

# Probe / scratch scripts (local verification artifacts, not project source)
probe_*.js
_scratch_*
# Agentic render intermediates (reconstructed each render)
workspace/jobs/*/render/_*
workspace/jobs/*/render/_intro_*
workspace/jobs/*/render/_outro_*

# Personal reference voice clips for cloning (gitignored — never committed)
input/voices/
# QA visual-verification temp frames (generated by qa:smoke / vision checks)
qa_frames/
# Scratch test/smoke-test files (local verification only, not project source)
*_test_advanced_fx.ts
_add_jobs.js
test-advanced.json
src/agentic/operations/_test_*.ts
# Agentic batch/sweep/bug-hunt scratch scripts (local verification runs, not source)
input/scripts/agentic-scripts-*.json
input/scripts/asset-sweep.json
input/scripts/bug-hunt-*.json
input/scripts/five-min.json
input/scripts/flux3-sweep.json
input/scripts/fx-sweep.json
input/scripts/stock-images-sweep.json
input/scripts/input-scripts.local.json
scripts/fetch-stock-images.sh
# Stray shell artifacts and temp dirs
$null
empty-dir-*/
# In-repo Python venv for the vendored speech backend (created via
# `python -m venv venv` + `pip install -r src/speech/requirements.txt`).
# Heavy + machine-specific — NEVER commit. The code lives in src/speech/ (tracked).
venv/
# (MIT, upstream jamiepine/voicebox, vendored 2026-07-20 commit 52f8d8d).
# Runtime artifacts inside it are excluded via src/speech/.gitignore.
# Regenerated by scripts/vendor-real-voice-backend.mjs from /c/one/voicebox.

# Vendored backend RUNTIME DATA (sqlite db, profiles, generated audio cache).
# The speech backend writes its data dir relative to cwd (src/), which would
# create src/data/voicebox.db. We redirect it via VOICEBOX_DATA_DIR to a
# gitignored cache path, but ignore the leak too as defense-in-depth.
src/data/
*.db
voicebox.db

# Python (bytecode, caches, virtualenvs) — regenerable, machine-specific
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/



# Autonomous Remotion codegen (generated, regenerable)
remotion-generation/
workspace/remotion-generation/

# ── Job runtime artifacts (regenerated per run; never commit) ──
/archive/
/audio/
/plan.json
/scene-data.json
/revision-state.json
/input/scripts/agentic-scripts.json
# Bundled OFFLINE pack ships WITH the repo (production guarantee) — exempt from *.mp4 / videos/ rules
!assets/bundled/
!assets/bundled/videos/
!assets/bundled/videos/*.mp4
