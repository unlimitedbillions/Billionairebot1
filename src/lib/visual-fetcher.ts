/**
 * Re-export shim — keeps backward compatibility with all existing
 * imports of 'src/lib/visual-fetcher' / 'src/lib/visual-fetcher.js'.
 *
 * The implementation has been split into sub-modules under
 * src/lib/visual-fetcher/ for better organization.
 */
export * from './visual-fetcher/index.js';
